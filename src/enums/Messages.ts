export enum Informative {
    Waiting = "Espere un momento por favor",
    EmptyTable = "Tabla sin registros",
}

export enum Warning {
    CheckCredentials = "Favor de revisar las credenciales",
}

export enum Errores {
    Error = "Ha ocuriddo un error, favor de reintentar",
}